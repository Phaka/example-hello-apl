⍝ A minimal APL program demonstrating basic output
⍝ The ⎕ symbol represents the system command interface in many APL implementations
⍝ This simple program uses ← for assignment and '' for string literals

'Hello, World!'
